import { makePolygon } from "../../makeSvgElements/makePolygon";

export default function resizePolygon(polygon: any, x: number, y: number, x0: number, y0: number) {
    let resizedPolygon = polygon;

    

    return resizedPolygon;
}
