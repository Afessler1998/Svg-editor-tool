export default interface transforms {
    translateX: number,
    translateY: number,
    skewX: number, 
    skewY: number,
    scaleX: number,
    scaleY: number,
    rotate: number,
    rotateX: number,
    rotateY: number,
}